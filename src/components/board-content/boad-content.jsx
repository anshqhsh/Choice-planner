import React from 'react';
import styles from './boad-content.module.css';

const BoadContent = ({ onSearch }) => {
  return <header className={styles.header}>Hello</header>;
};

export default BoadContent;
